import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  eventName: {
    color: '#FDFCFE',
    fontWeight: '600',
    fontSize: 24,
    marginTop: 40,
  },
  eventDate: {
    color: '#FDFCFE',
    fontSize: 20,
  },
});

export default styles;
